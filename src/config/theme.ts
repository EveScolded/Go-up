import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#78A1BB",
      dark: "#283044",
      contrastText: "#fff",
    },
    secondary: {
      main: "#EBF5EE",
      contrastText: "#fff",
    },
  },
});

export default theme;

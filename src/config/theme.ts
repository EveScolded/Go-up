import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#213158",
      dark: "#17213D",
      contrastText: "#fff",
    },
    secondary: {
      main: "#EBF5EE",
      contrastText: "#fff",
    },
  },
});

export default theme;
